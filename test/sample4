
int main(int a, int x)
{
  x = 5;

  x = x + 2;

  if (x == 7) {
    x = x + 1;
  } else {
    x = x + 1;
  }

  while (x != 10) {
    x = x + 1;
  }

  testfunc(x);

  return 1;
} 

int testfunc(int x)
{
	return 1;
}
